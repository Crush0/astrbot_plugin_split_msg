{
    "words_count_threshold": {
        "type": "int",
        "description": "消息分块阈值，默认值为 15",
        "default": 15
    },
    "enable": {
        "type": "bool",
        "description": "是否启用插件，默认值为 true",
        "default": true
    }
}